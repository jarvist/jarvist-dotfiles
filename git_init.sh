git config --global user.name "Jarvist Moore Frost"
git config --global user.email "jarvist@fakemail.fake"
git config --global credential.helper cache
git config --global credential.helper 'cache --timeout=3600'

